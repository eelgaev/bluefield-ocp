#% desc: Enable the EPEL repository
RUN cd /tmp && \
  curl -O https://dl.fedoraproject.org/pub/epel/epel-release-latest-{{ RHEL_MAJOR }}.noarch.rpm && \
  rpm -ivh epel-release-latest-{{ RHEL_MAJOR }}.noarch.rpm && \
  rm epel-release-latest-{{ RHEL_MAJOR }}.noarch.rpm
